#include <algorithm>
#include <iostream>
using namespace std;
int n;

void work() {
    cin >> n;
}

int main() {
    ios::sync_with_stdio(false);
    int T;
    cin >> T;
    while(1)
        work();
    return 0;
}
