#include <iostream>
#include <algorithm>
#include <iterator>
using i64 = long long;

void work() {

}

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);
  int T;
  std::cin >> T;
  while (T--)
    work();
  return 0;
}
